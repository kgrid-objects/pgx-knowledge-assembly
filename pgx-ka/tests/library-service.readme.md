To run library service tests, first install pgx-ka dependencies by running `npm install` in the root of the knowledge assembly (pgx-ka/).

Then run `npx mocha tests/unit/library-service.test` in the same folder.