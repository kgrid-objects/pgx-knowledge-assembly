To run plugin service tests, first install pgx-ka dependencies by running `npm install` in the root of the knowledge assembly (pgx-ka/).

Then run `npx mocha tests/unit/plugin-service.test` in the same folder.